
export const metadata = { title: "About — summa.vision" }

# About

**summa.vision** — статический сайт с чартами и разделами (MVP).

- Next.js App Router
- Static export (SSG)
- Core TypeScript (без React) + реестр рендереров
